namespace = anatolia_expanded

country_event = {
	id = anatolia_expanded.1
	title = anatolia_expanded.1.t
	desc = anatolia_expanded.1.d
	picture = HORDE_ON_HORSEBACK_eventPicture

	fire_only_once = yes

	trigger = {
		primary_culture = turkish
		ai = no
		NOT = { exists = RUM }
	}
	
	mean_time_to_happen = {
		days = 1
	}
		
	option = {
		name = anatolia_expanded.1.a
		release_all_subjects = yes
		change_tag = RUM
			RUM = {
				if = {
					limit = {
						NOT = { government = monarchy }
						is_revolutionary = no
					}
					change_government_to_monarchy = yes
				}
				if = {
					limit = { is_revolutionary = no }
					add_government_reform = ottoman_government
				}
			}
			remove_non_electors_emperors_from_empire_effect = yes
			if = {
				limit = {
					NOT = { government_rank = 2 }
				}
				set_government_rank = 2
			}
			every_province = {
				limit = {
					culture = turkish
				}
				add_permanent_claim = RUM
			}
			add_country_modifier = {
				name = "centralization_modifier"
				duration = 7300
			}
			if = {
				limit = { has_custom_ideas = no }
				country_event = { id = ideagroups.1 } #Swap Ideas
			}
			add_prestige = 25
			change_technology_group = ottoman
			change_unit_type = ottoman
	}
	option = {
		name = anatolia_expanded.1.b
		release_all_subjects = yes
		change_tag = RUM
			RUM = {
				if = {
					limit = {
						NOT = { government = republic }
						is_revolutionary = no
					}
					change_government_to_republic = yes
				}
				if = {
					limit = { is_revolutionary = no }
					add_government_reform = peasants_republic
				}
			}
			remove_non_electors_emperors_from_empire_effect = yes
			if = {
				limit = {
					NOT = { government_rank = 2 }
				}
				set_government_rank = 2
			}
			every_province = {
				limit = {
					culture = turkish
				}
				add_permanent_claim = RUM
			}
			add_country_modifier = {
				name = "centralization_modifier"
				duration = 7300
			}
			if = {
				limit = { has_custom_ideas = no }
				country_event = { id = ideagroups.1 } #Swap Ideas
			}
			add_prestige = 25
			change_technology_group = ottoman
			change_unit_type = ottoman
	}
	option = {
		name = anatolia_expanded.1.c
	}
}
